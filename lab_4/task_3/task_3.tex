% Preamble
\documentclass[11pt]{article}

% Packages
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[polish]{babel}
\usepackage{amsmath}
\usepackage{algorithm2e}
\usepackage{algpseudocode}
\usepackage[margin=1in]{geometry}

% Translations
\SetKw{KwTo}{do}
\SetKw{Length}{długość}
\SetKw{ToNumber}{liczba}
\SetKwFor{For}{Dla}{wykonuj}{koniec}
\SetKwFor{While}{Gdy}{wykonuj}{koniec}
\SetKw{Break}{Wyjdź z pętli}
\SetKw{Continue}{Przejdź do kolejnej iteracji pętli}
\SetKwIF{If}{ElseIf}{Else}{Jeżeli}{to}{w przeciwnym razie jeżeli}{w przeciwnym razie}{koniec}

% Document
\begin{document}

    \section{Algorytm A*}

    \begin{algorithm}[H]
        \SetAlgoLined
        Start\\

        Wczytaj graf skierowany $graph$ w postaci macierzowej, $source$, $destination$ oraz funkcję heurystyczną przewidywanej odeległości $approximate\_distance$\\

        $vertex\_count \gets$ \textbf{ilość wierzchołków} $graph$\\

        $weights \gets$ tablica długości $vertex\_count$\\
        $history \gets$ tablica długości $vertex\_count$\\

        \For{$i \gets 0$ do $vertex\_count$}{
            $weights[i] \gets$ \textbf{para} $\infty$, $\infty$\\
            $history[i] \gets brak$\\
        }

        $pending\_vertices \gets$ set pojemności $vertex\_count$\\

        Dodaj $source$ do $pending\_vertices$\\
        $weights[source] \gets$ \textbf{para} $0$, $approximate\_distance(source)$\\

        \While{\Length $pending\_vertices > 0$}{
            $vertex \gets$ element z $pending\_vertices$ o najmniejszej wartości $weights[vertex][1]$\\

            \If{$vertex = destination$}{
                \Break
            }

            Usuń wartość $vertex$ z $pending\_vertices$\\

            \For{$neighbor \gets 0$ do $vertex\_count$}{
                $distance \gets graph[vertex][neighbor]$\\

                \If{$distance \leq 0$}{
                    \Continue
                }

                $calculated\_weight \gets weights[vertex][0] + distance$\\

                \If{$calculated\_weight < weights[neighbor][0]$}{
                    $weights[neighbor] \gets$ \textbf{para} $calculated\_weight$, $calculated\_weight + approximate\_distance(neighbor)$\\
                    $history[neighbor] \gets vertex$\\
                    Dodaj $neighbor$ do $pending\_vertices$\\
                }
            }
        }

        \If{\Length $pending\_vertices < 1$}{
            Wypisz komunikat o braku rozwiązań.\\
            Stop\\
        }

        $path \gets$ pusta lista\\
        $vertex \gets destination$\\

        \While{$vertex \neq -1$}{
            Wstaw wartość $vertex$ na pozycji $0$ do $path$\\
            $vertex \gets history[vertex]$\\
        }

        Wypisz ścieżkę $path$ oraz sumaryczny dystans $weights[destination][0]$\\

        Stop\\
        \label{alg:algorithm1}
    \end{algorithm}

\end{document}